## Test reporting requirements

When adding or changing tests, update the active task note under `docs/agent_notes/`.

The note must include:

- Test files added or modified.
- Exact commands run.
- Pass/fail results.
- Any skipped tests and why.
- Any test coverage gaps that should be addressed by a future agent.